{
    "name": "@jjdenhertog/open-spotify-sdk",
    "version": "1.0.4",
    "description": "A typescript SDK for the Open Spotify Web API",
    "scripts": {
        "deploy": "tsc && npm version patch --no-git-tag-version && cp package.json ./dist && cp README.md ./dist && cd dist && npm publish --access public",
        "build": "tsc"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/jjdenhertog/open-spotify-sdk"
    },
    "author": "jjdenhertog",
    "license": "Apache",
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=10.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.3.2",
        "typescript": "^5.1.6"
    },
    "publishConfig": {
        "access": "public"
    },
    "keywords": [
        "spotify",
        "open"
    ]
}
